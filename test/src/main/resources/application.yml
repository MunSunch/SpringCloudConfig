spring:
  profiles:
    active: prod
  application:
    name: calculator
  config:
    import: 'optional:configserver:http://localhost:8888'

management:
  endpoints:
    web:
      exposure:
        include: refresh

prop:
  value: 'aa'